/**
 * The React version string reported by `React.version` and the renderer.
 *
 * Bumped to 19.0.0 to reflect the React 19 feature surface provided by
 * `@nrbx/react`.
 *
 * @module ReactVersion
 * @internal
 * @packageDocumentation
 */

export default '19.0.0';
