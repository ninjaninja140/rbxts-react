/**
 * Tracks the current batch's configuration, such as how long an update should
 * suspend for if it needs to.
 *
 * @module ReactCurrentBatchConfig
 * @internal
 * @packageDocumentation
 */

const ReactCurrentBatchConfig = {
	transition: 0,
};

export default ReactCurrentBatchConfig;
